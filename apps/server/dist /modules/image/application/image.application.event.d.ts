export declare namespace ImageApplicationEvent {
    namespace ImageCreated {
        const key = "image.application.image.created";
        type Payload = {
            id: string;
            userId: string;
        };
    }
}
