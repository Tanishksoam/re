export declare namespace ContractApplicationEvent {
    namespace ContractCreated {
        const key = "contract.application.contract.created";
        type Payload = {
            id: string;
            userId: string;
        };
    }
}
