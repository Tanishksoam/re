export declare namespace CommunicationApplicationEvent {
    namespace CommunicationCreated {
        const key = "communication.application.communication.created";
        type Payload = {
            id: string;
            userId: string;
        };
    }
}
