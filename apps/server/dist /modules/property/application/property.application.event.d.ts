export declare namespace PropertyApplicationEvent {
    namespace PropertyCreated {
        const key = "property.application.property.created";
        type Payload = {
            id: string;
            userId: string;
        };
    }
}
