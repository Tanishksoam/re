export declare namespace FeedbackApplicationEvent {
    namespace FeedbackCreated {
        const key = "feedback.application.feedback.created";
        type Payload = {
            id: string;
            userId: string;
        };
    }
}
